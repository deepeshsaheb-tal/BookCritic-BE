apiVersion: v1
kind: Service
metadata:
  name: bookcritic-backend
  namespace: bookcritic
  labels:
    app: bookcritic-backend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: bookcritic-backend
